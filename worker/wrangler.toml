# Only needed if you deploy from the command line (npx wrangler deploy).
# Pasting worker/contact.js into the Cloudflare dashboard works just as well.
name = "portfolio-contact"
main = "contact.js"
compatibility_date = "2026-09-01"

[vars]
TO_EMAIL = "ruyinqc@gmail.com"
ALLOWED_ORIGINS = "https://ruyinqc.github.io,http://localhost:8080"

# The API key is a secret, never put it in this file:
#   npx wrangler secret put RESEND_API_KEY
